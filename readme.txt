git config --global user.name "sdf"
git config --global user.email @qq.com

git config --global user.name "dxl"
代理：
git config http.proxy http://127.0.0.1:7897
git config http.proxy https://127.0.0.1:7897
